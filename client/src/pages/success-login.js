import React, { useState } from "react";
import auth from './../components/auth';

export const SuccessLogin = (props) => {
    
    return(
        <div>
            aii yo got in
        </div> 
    );
    
}